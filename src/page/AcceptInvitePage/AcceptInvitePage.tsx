import { Container } from '@mui/material';

import { AcceptInvite } from '@container';

export const AcceptInvitePage = () => (
    <Container sx={{ height: '100%' }}>
        <AcceptInvite />
    </Container>
);
