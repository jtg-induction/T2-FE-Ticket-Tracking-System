import { Stack } from '@mui/material';

import { ProjectList } from '@container';

export const ProjectPage = () => (
    <Stack height="100%" bgcolor="background.paper">
        <ProjectList />
    </Stack>
);
