import { Stack, styled } from '@mui/material';

export const AuthPageRoot = styled(Stack)(() => ({
    height: '100%',
    width: '100%',
    display: 'flex',
    flexDirection: 'row',
    margin: 'auto',
}));
